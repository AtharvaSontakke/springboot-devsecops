apiVersion: v1
kind: Service
metadata:
  name: springboot-devsecops-service
spec:
  selector:
    app: springboot-devsecops
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: NodePort
